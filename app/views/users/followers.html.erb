<div class = "container">
  <%= render "shared/photos_tab"%>
</div>

<div class="list-group">
  <%current_user.followers.each do |follower|%>
  <%=link_to follower.username, followers_path(follower.username), class:"list-group-item list-group-item-action" %>
 <%end%>
</div>